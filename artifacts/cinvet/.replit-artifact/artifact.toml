kind = "web"
previewPath = "/"
title = "Cinvet Hospital Veterinário"
version = "1.0.0"
id = "artifacts/cinvet"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19366

[services.development]
run = "pnpm --filter @workspace/cinvet run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/cinvet", "run", "build" ]
publicDir = "artifacts/cinvet/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19366"
BASE_PATH = "/"
