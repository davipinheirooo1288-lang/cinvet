{
  "name": "cinvet-site",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @workspace/cinvet run dev",
    "build": "pnpm --filter @workspace/cinvet run build",
    "typecheck": "pnpm --filter @workspace/cinvet run typecheck",
    "preview": "pnpm --filter @workspace/cinvet run serve"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
